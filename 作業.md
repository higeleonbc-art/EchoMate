# EchoMate 開発ロードマップ（Claude Code 連携用）

> [!IMPORTANT]
> **本プロジェクトの最重要原則（作業フロー）**
> 1. AI は直接ソースコードを書き換えないでください。
> 2. どんなに軽微な修正（1行の追加や設定変更）であっても、まず本ファイル（作業.md）に「実施計画」を追記してください。
> **このルールは、セッションを跨いで常に適用されます。**

# Phase 1: コア機能の実装と近代化 ✅
# Phase 2: AI クオリティ向上と堅牢化 ✅
# Phase 3: 人間らしい気配と VLM の深化 ✅

---

# Phase 4: AI 品質と安定性の向上 ✅
- **4.1 AI 判定ロジックの堅牢化 (ai.py)**: タイムアウト延長(25s)・must_start 制約に【厳命】追加済み。
- **4.2 イベント・スロットリングの実装 (main.py)**: `is_busy()` (> 0)、`EVENT_COOLDOWN_SEC=10s`、`priority >= 2` ベース条件適用済み。
- **4.3 分析データの堅牢化 (patron_analyzer.py)**: JSON 抽出ロジック適用済み。
- **4.4 キャラクターの刷新 (characters.json)**: 快活・応援団長ミチコへ刷新済み。
- **4.5 設定のチューニング (cv_config.json)**: 各クールダウン最適化済み。

# Phase 5: 特定プロセスの音声キャプチャ & 自己応答防止 ✅（接続完了）
- **5.1 audio_capture_pro.py 作成**: `AudioCaptureProWrapper` 実装済み。
- **5.2 audio_detector.py 改修**: `target_pid` / `exclude_pids` / `voice_output` パラメータ追加、`_audio_loop_pac()` 実装済み。
- **5.3 自己応答防止ガード**: フォールバックモード時限定で `voice_output.is_speaking` チェック実装済み。
- **5.4 gui.py 接続**: `get_selected_pid()` 追加・`EchoMate` 起動時に `audio_target_pid` を渡す経路を接続済み。
- **5.5 main.py 接続**: `audio_target_pid` / `audio_exclude_pids` → `AudioDetector` へ引き渡し済み。

---

# ✅ コード品質チェック結果（対応完了）

> 2026-04-17 全体品質チェックで検出・修正済み。

- **C-1**: `audio_capture_pro.py` を実 API (`ProcessAudioCapture(pid, on_data)` + `on_data` コールバック → キュー) で完全再実装。`audio_detector.py` の `_audio_loop_pac` も単一 PID 渡しに修正。
- **W-1**: `main.py` のクールダウンをイベント種別ごとに分離（kill/death=5s、low_hp/big_play=10s）。
- **W-2**: `gui.py` に `ServiceManager.get_voicevox_pids()` を追加し、起動時に `audio_exclude_pids` として `EchoMate` へ渡すよう接続。

---

# Phase 6: 未来への展望（今後予定）
- **性能監視**: AI の思考速度や API 応答時間をリアルタイムで可視化するダッシュボード。
- **マルチモーダル強化**: 指示に対して画面内の特定箇所をより高精度に読み取れるように。
- **自動クリップ作成**: 会話が盛り上がったシーンを自動で動画保存。
