# EchoMate 追加機能仕様書（Claude Code向け）

## 【実装完了】これまでのタスク（1〜7）
以下の機能はすべて実装・検証済みです。

1.  **質問・掘り下げの動的付与**: 会話の文脈に応じた自発的な問いかけ。
2.  **フィラー（相槌）の先行再生**: LLM生成を待たずに「えーっと」「なるほど」等を再生。
3.  **再生キャンセル（Barge-in）**: プレイヤーの発言時にAIの音声を中断。
4.  **過去の話題（recent_topics）の掘り起こし**: RAGによる話題の再利用。
5.  **感情別 VOICEVOX スタイル切り替え**: テンションに連動した声色変化。
6.  **Webソース型学習機能**: プレイヤー提供のURLからゲーム知識をスクレイピング・要約。
7.  **「良き隣人」成長システム**:
    *   親密度（Bond Level）の動的蓄積とGUI表示。
    *   プレイスタイル分析結果のAI人格（トーン）への反映。
    *   エピソード記憶の優先抽出。
    *   マルチスレッドセーフな I/O マージロジック（Safe Save）。

---

## 【改善建議タスク】システムの堅牢化と品質向上
プロジェクト・クオリティ・レビュー（2026-04-16）に基づいた、次期フェーズでの実施を推奨する改善案です。

### 1. データの永続化と安全性（最優先）
*   **アトミック書き込みの導入**: `user_profile.py` の `save()` メソッドを、一時ファイルへの書き出し → リネーム（`os.replace`）方式に変更し、書き込み中のフリーズによる JSON 破損を完全に防ぐ。
*   **履歴データの SQLite 移行**: `user_profile.json` 内の `memorable_episodes` や `growth_observations` は増大し続けるため、これらを `PatronDB` (SQLite) へ移行し、メモリ負荷と I/O 効率を改善する。

### 2. コンピュータビジョン（CV）の信頼性向上
*   **時系列フィルタのプリセット適用**: 誤検知が目立つ領域（キルフィード等）に対し、`cv_config.json` 内で `min_hits: 2` などを標準設定とし、瞬間的なノイズをカットする。
*   **オンデマンド Vision 解析のフォールバック**: オーディオトリガーによる VLM 呼び出しがタイムアウトやエラーになった際の例外処理を強化し、AI が「沈黙」せず別の話題へ遷移するようにする。

### 3. UX・フィードバックの強化
*   **思考ステータスの可視化**: LLM や VLM の呼び出し中に GUI 上で「AI 思考中...」といったインジケーターを表示し、フリーズしていないことをユーザーに伝える。
*   **マルチモニター対応**: `VisionAnalyzer` がプライマリモニター以外もキャプチャできるようにモニター選択機能を GUI に実装する。

---

## 【将来の実装予定】配布に向けたパッケージング準備
*   **インストーラ・導線整備**: Ollama や VOICEVOX が未インストールの場合、公式ダウンロードページをブラウザで開くボタンを実装する。
*   **ビルド自動化**: `PyInstaller` を使用した `EchoMate.exe` へのビルド構成の確立。
*   **README の整備**: 日本語/英語でのセットアップガイド作成。

---

---

## 【近代化タスク】LLMモデル更新・ライブラリ刷新（2026-04-17）
システムのパフォーマンス向上と最新モデルへの対応を目的としたタスクです。

### 1. LLM / VLM モデルのアップグレード
*   **LLM**: `qwen2.5:7b` から `qwen3:8b` へ変更。日本語の自然さと応答速度のバランスを最適化する。
    *   ~~`llama-3.1-japanese-instruct:8b`~~ → Ollama に存在しないモデル名だったため `qwen3:8b` に修正。
*   **VLM (Llama 3.2 Vision)**: `moondream` から `llama3.2-vision:11b` へ変更。ゲーム画面の解析精度を向上させる。

### 2. ライブラリの刷新と環境管理
*   **HTTP クライアントの移行**: `requests` から `httpx` へ移行。将来的な非同期化 (asyncio) への布石とし、タイムアウト管理を厳格化する。
*   **音声ライブラリの安定化**: `pyaudio` から `pyaudiowpatch` へ移行し、Windows の WASAPI ループバックデバイスの列挙を安定させる。
*   **環境変数の導入**: `python-dotenv` を導入し、LLMモデル名や API URL を `.env` ファイルで一括管理する。

### 3. ログ管理の強化
*   **ログローテーションの実装**: `main.py` の `_setup_logging` を修正し、`logging.handlers.RotatingFileHandler` を使用して `echomate.log` が無限に肥大化しないように制限（例: 10MB/5世代）を加える。

### 4. その他クリーンアップ
*   **不要ファイルの削除**: ルートディレクトリにある一時的なパッチファイル（`*.patch`）等を削除する。

---

## メモ：Claude Code への指示時の注意
この `作業.md` を Claude Code に読み込ませ、「【改善建議タスク】の 1 を実装してください」といった形で指示を出してください。実装時は常に `main.py` のメインループをブロッキングしないよう、スレッドセーフな実装を徹底してください。
また、「【近代化タスク】」については、一部のファイル（ai.py, voice.py等）が既に Antigravity によって `httpx` への置換が始まっている可能性があるため、差分を確認しながら進めてください。

---

## 【最終調整】近代化タスクの修正・補完（2026-04-17）

### 実施済み修正

1.  **voice.py の更新漏れ解消**（近代化タスク 2 の対象だったが当初スキップされていた）
    *   `requests` → `httpx` に移行（`_synthesize` メソッド内の `audio_query` / `synthesis` 呼び出し）
    *   `import pyaudio` を `pyaudiowpatch` 優先フォールバック方式に変更：
        ```python
        try:
            import pyaudiowpatch as pyaudio
        except ImportError:
            import pyaudio
        ```

2.  **幻覚モデル名の修正**
    *   LLM: `llama-3.1-japanese-instruct:8b` → Ollama に存在しないモデル名だったため **`qwen3:8b`** に修正。
    *   `ai.py` のデフォルト定数・`.env` テンプレート・本ドキュメントのすべてを `qwen3:8b` に統一。
    *   VLM: `llama3.2-vision:11b` は実在が確認されているため変更なし。

3.  **`get_followup()` の未使用 `memory` パラメータ修正** (`ai.py`)
    *   `memory` をプロンプトに注入するよう修正し、recent_topics 等の文脈が step2/3 にも流れるようにした。
