# EchoMate - 依存パッケージ
# インストール: pip install -r requirements.txt

# ローカル音声認識（faster-whisper / オフライン動作）
faster-whisper>=1.0.0

# HTTP クライアント（VOICEVOX API + Ollama HTTP API）
# requests から httpx へ移行（非同期化への布石・タイムアウト管理厳格化）
httpx>=0.27.0

# 環境変数管理（.env でモデル名・API URL を一括管理）
python-dotenv>=1.0.0

# 数値計算（VAD・RMS 計算）
numpy>=1.24.0

# 音声入出力（WASAPI ループバック対応版を優先）
# Windows: pip install pyaudiowpatch
# Linux / macOS: pip install pyaudio
pyaudiowpatch>=0.2.12.6; sys_platform == "win32"
pyaudio>=0.2.14; sys_platform != "win32"

# ── OpenCV 画面検出（任意）────────────────────────────────────
# pip install opencv-python mss numpy
opencv-python>=4.8.0
mss>=9.0.1

# ── セットアップウィザード（Windows のみ）───────────────────
# pip install pywin32
pywin32>=306; sys_platform == "win32"

# ── GUI（gui.py）────────────────────────────────────────────
# プロセス列挙
psutil>=5.9.0
# 画像表示（ROI セレクター）
Pillow>=10.0.0
