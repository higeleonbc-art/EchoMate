# LoL ADC Coach - 依存パッケージ
# インストール: pip install -r requirements_coach.txt

# HTTP クライアント（Riot API / LCU / Live Client / Ollama）
httpx>=0.27.0

# 環境変数管理（RIOT_API_KEY 等）
python-dotenv>=1.0.0

# GUI (Edge WebView2 ベース。Windows標準ランタイムを使用)
# pywebview を導入してから coach_gui.py をメインエントリにする
pywebview>=5.0

# コーチング動画 transcript 取得（coach_corpus.py 用）
youtube-transcript-api>=0.6.0

# Live Overlay は tkinter（標準ライブラリ）で実装。VOICEVOXは不採用。
